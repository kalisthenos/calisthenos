# app/components/ — współdzielone komponenty UI

Komponenty React reużywane przez trasy trenera i podopiecznego. Bez logiki
domenowej — czysta prezentacja + drobne interakcje (modale, providery kontekstu).

| Plik | Eksporty | Rola |
|---|---|---|
| `icons.tsx` | `Icons` (40+ ikon: `Dashboard`, `Users`, `Library`, `Play`, `Check`, `Trash`, `Calendar`, `Body`, `Trend`, `Card`…), `makeIcon` | Centralny zestaw ikon SVG line-style (`currentColor`, rozmiar przez `font-size`). |
| `modal.tsx` | `Modal({ open, onClose, title, wide?, children })` | Dialog na natywnym `<dialog>`; blokada scrolla, ESC, backdrop. |
| `confirm-provider.tsx` | `ConfirmProvider`, `useConfirm()`, `useAlert()`, `ConfirmSubmitButton` | Potwierdzenia/alerty przez kontekst; przycisk pytający przed submitem formularza. |
| `toast-provider.tsx` | `ToastProvider`, `useToast()`, `ToastTone` | To'sty (success/error/info), auto-dismiss ~3s. |
| `user-menu.tsx` | `UserMenu({ displayName })` | Chip użytkownika z menu: przełącznik motywu (cookie 1 rok) + wylogowanie (`Form` POST). |
| `pagination.tsx` | `Pagination({ page, totalPages, total?, … })`, `parsePage(searchParams)` | Paginacja z prev/next i numerami; zachowuje query params. |
| `file-dropzone.tsx` | `FileDropzone({ name, label, kind: "video"\|"image", … })`, typ `FileKind` | Wybór pliku drag&drop z podglądem i walidacją MIME/rozmiaru. Plik jedzie razem z formularzem — używany tam, gdzie formularz niesie JEDEN plik (sylwetka, demo ćwiczenia). |
| `video-upload-field.tsx` | `VideoUploadField({ name, label, maxBytes, idSuffix, initialFileId?, onStateChange })`, typ `VideoUploadState` | Pole nagrania serii wysyłające plik OD RAZU po wyborze na `/upload/wideo` (XHR — `fetch` nie raportuje postępu wysyłki), z procentem, anulowaniem i stanem „wgrane". W formularzu zostaje tylko ukryty `fileId`. Postęp rysowany jako gradient tła wspólnej skorupy 36 px, żeby lista serii nie skakała przy zmianie stanu. |
| `copy-button.tsx` | `CopyButton({ value, variant?, label?, … })` | Kopiowanie do schowka z feedbackiem (Clipboard API + fallback). |
| `exercise-fields.tsx` | `CategoryPicker({ categories, selected })` | Multi-select kategorii ćwiczeń (pigułki/checkboxy). |
| `photo-card.tsx` | `PhotoCard({ id, url, takenOn, view, note, onOpen })`, `BODY_VIEW_LABELS` | Kafelek zdjęcia sylwetki 3:4 otwierający lightbox. |
| `photo-lightbox.tsx` | `PhotoLightbox({ photos, currentId, onClose, onNavigate, deleteAction? })`, `LightboxPhoto` | Pełnoekranowy podgląd zdjęć: nawigacja strzałkami, pobieranie, opcjonalne usuwanie. |
| `body-photo-compare.tsx` | `SideBySideSection({ pairs, onOpenPhoto })`, `ResolvedPair` | Porównanie "przed/po" zdjęć sylwetki wg widoku (front/side/back). |
| `video-modal.tsx` | `VideoButton({ src, title, label?, size? })`, `VideoModal({ src, title, onClose })` | Przycisk + modal odtwarzania wideo. |
| `stat-widgets.tsx` | `Heatmap`, `Sparkline`, `SegmentedBar`, `SegmentedBarLegend`, `BarSegment` | Wykresy do dashboardów statystyk (heatmapa GitHub-style, sparkline, pasek segmentowy). |
| `progression-charts.tsx` | `ProgressionLineChart`, `VolumeBars`, `ComparisonChart`, `ComparisonChartLegend`, `ProgressionStatusBadge`, `StatusSummaryBar`, `sparkStrokeForStatus` | Wykresy feature'u „Progresja" zbudowane na **visx** (SVG, responsywne, tree-shakeable): liniowy rekord-w-czasie (kropki kolorowane wg RPE/statusu, legendy, osie), słupki objętości. `ProgressionLineChart` z legendą RPE w wykresie (pojedynczy punkt rysuje jako kropkę; „za mało danych" tylko przy 0 punktach); `ComparisonChart` z osią % i prowadnicą na dolnym poziomie. Tooltipy działające na hover i dotyk. `StatusSummaryBar` nad listą (pasek podsumowania statusów); `sparkStrokeForStatus` mapuje trend do koloru. Czysta prezentacja — bez fetchowania. |
| `consultation-form.tsx` | `ConsultationForm({ defaultValue?, defaultScheduledAt? })`, `ConsultationFormDefaultValue`, `ConsultationFormItem` | Formularz dokumentowania konsultacji (termin+czas trwania, link, okres, tytuł, podsumowanie, dynamiczna lista punktów „do poprawy"). Nie renderuje `<Form>` — owija go trasa-rodzic. |
| `schedule-form.tsx` | `ScheduleForm({ defaultValue?, defaultStartsOn })`, `ScheduleFormDefaultValue`, `Cadence` | Formularz reguły harmonogramu cyklicznego (częstotliwość radio-pills, dzień tygodnia/miesiąca, godzina, czas trwania, start, stały link). Kontrolowany (cadence przełącza pole kotwicy). Nie renderuje `<Form>` — owija go trasa-rodzic. |
| `month-calendar.tsx` | `MonthCalendar({ year, month0, todayDay, days, selected, onSelect, prevHref, nextHref })`, `DaySummary` | **Jedna siatka miesiąca dla obu ról** (kalendarz trenera i podopiecznego wyglądają identycznie). Tydzień od poniedziałku, „dziś" jako żeton, kropka/licznik dnia kolorowane tonem statusu. Prezentacja — stan `selected` trzyma rodzic. |
| `consultation-status-badge.tsx` | `StatusBadge({ label, tone })` | Badge statusu terminu na sygnaturowym `.badge` (mono, kropka wiodąca); kolory z tonu `consultation-status`. Wspólny dla wszystkich widoków konsultacji. |
| `consultation-row.tsx` | `ConsultationRow({ to, lead, title, sub?, label, tone })` | Wiersz agendy terminu (link `list-row`): mono data/godzina + tytuł + `StatusBadge` + chevron. Wspólny dla list trenera i podopiecznego. |
| `consultation-alert.tsx` | `ConsultationAlert({ data })` | Jednolity komunikat zwrotny akcji (błąd/sukces) na klasach `.alert`. Zastępuje powielane inline `<p>/<output>` w trasach konsultacji. |
| `trainee-occurrence-actions.tsx` | `TraineeOccurrenceActions({ consultationId, compact? })` | Akcje podopiecznego na terminie: Potwierdzam / Poproś o zmianę (textarea) / Odrzuć. POST → `respondToOccurrence`. Wspólny dla karty „najbliższy", kart dnia i szczegółów. |
| `skill-tree.tsx` | `SkillTreeView({ tree, hrefForNode })`, `VariationLadder({ variations })` | `SkillTreeView`: **monument umiejętności** — warstwy tierów jedna na drugiej, fundament (`basic`) na dole, `expert` na szczycie; płyty idą **pełną szerokością planszy** (wcięcie wycofane 2026-07-25), każda z railem (etykieta + licznik `4/6`) i rzymskim numerem `I–IV` wykutym w rogu; płyta `expert` jest atramentowa. Nad planszą nagłówek postępu (opanowane `n/m`, najwyższy zdobyty tier, w toku), a na wysokości najwyższego zdobytego tieru **linia szczytu** (`--accent` + plakietka „Twój szczyt"). Wszystko liczą czyste funkcje: `~/lib/skill-pyramid` (`buildPyramid` → `orderAndPlace` → `layoutPyramid`) i `~/lib/skill-pyramid-routing` (`routeEdges` + `edgePathD`). Karty pozycjonowane absolutnie — oś X w jednostkach `0..VIEW_W` (procenty), oś Y w px 1:1 — a krawędzie to **ortogonalne łamane** z `preserveAspectRatio="none"` i `vector-effect="non-scaling-stroke"`, w warstwie **pod** kartami. Zero pomiaru DOM, zero `useEffect`, SSR-safe. **Kierunek zależności** kodują dwa znaczniki DOM na karcie (nie `<marker>`, bo oś X jest rozciągana): stopka na górnej krawędzi („z tego coś wyrasta") i grot na dolnej („tutaj wchodzą prereki"); każda krawędź biegnie od stopki do grotu. Dwa niezależne kodowania: **tier → ciężar karty** (`data-tier`: well → hairline → ramka 1.5px → inwersja atramentowa), **stan → akcent** (`data-state`: kropka, rowek postępu; lime zarezerwowany dla postępu). Krawędź odwrócona (prereq wyżej niż zależna) rysowana `--warn` kropkowaną prostą. Każda karta to `Link` do `hrefForNode(skillId)`; pusty stan + legenda z regułą kierunku. Style: sekcja „PIRAMIDA UMIEJĘTNOŚCI" w `app/styles/tokens.css`. `VariationLadder`: growy, pionowy tor wariantów per umiejętność (drill-in) — kroki sortowane po `ordinal` na pionowej ścieżce z łącznikiem, stany done (`--ok` + ptaszek) / current (`--accent` + „Tu jesteś") / locked (przygaszone), etykiety mono, ten sam język wizualny co piramida. Czysta prezentacja, read-only — bez fetchowania, bez `Link`. |
| `feature-request-badge.tsx` | `FeatureRequestBadge({ status })` | Plakietka statusu zgłoszenia („Pomysły") na sygnaturowym `.badge` (mono, kropka wiodąca); etykieta i ton z `statusPresentation` (`~/lib/feature-request-types`), więc status wygląda tak samo u autora i u trenera. |
| `tier-badge.tsx` | `TierBadge({ tier })` | Plakietka poziomu trudności umiejętności w idiomie `.badge` (mono, wersalik). Bez koloru per tier. |
| `trainee-stats.tsx` | `HeroStatsCard`, `ThisWeekCard`, `ActivityHeatmapCard`, `EffortBalanceCard`, `WrappedListRow` | Prezentacyjne karty statystyk pulpitu podopiecznego (hero, ten tydzień, heatmapa, effort balance RPE, lista Wrapped). Bez fetchowania. |
| `trainee-health.tsx` | `HealthTilesCard`, `PlateauCard`, `PlanUsageCard`, `CoverageCard`, `TagDistributionCard` | Prezentacyjne karty cockpitu zdrowia klienta (widok trenera): health tiles, plateau, wykorzystanie sesji planu z sumami, coverage video/zdjęcia, rozkład tagów. Stoją na kształtach z kontraktu (`TraineeOverviewView`, re-eksporty w `lib/stats`); `PlanUsageCard` bierze `ActivePlanUsageView \| null` i sam sumuje `doneCount` na liczbę sesji, bo `PlanTotals` jej nie niesie (luka L S5-3). Karta heatmapy (`ActivityHeatmapCard`) **zniknęła** — kontrakt nie ma mapy aktywności dla widoku trenera (luka L S5-1); wersja pulpitu podopiecznego żyje w `trainee-stats.tsx`. Bez fetchowania. |
| `list-controls.tsx` | `ListControls({ spec, state, searchPlaceholder? })` | Współdzielony pasek kontrolek listy: szukajka (opcjonalna) + dropdown sortu + chipy filtrów. URL-driven (server-side); debounce 300 ms na zmianę; działa bez JS (`<noscript>`). Przyjmuje `ListControlsSpec` + `ListControlsState` z `~/lib/list-params`. |
| `exercise-progression-panel.tsx` | `ExerciseProgressionPanel` | Panel szczegółów progresji ćwiczenia: KPI (PR all-time, zmiana, sesje, RPE), przełącznik „Okres" (`?zakres=`) + wykres rekordu (`ProgressionLineChart`) + objętość (`VolumeBars`). Rola-agnostyczny — dane przekazywane przez propsy. |
| `progression-list.tsx` | `ProgressionList` | Lista progresji ćwiczeń z trybem porównania (wybór ≥2 → redirect na `/porownanie`): sparkline + status + PR per wiersz; rola-agnostyczna — bez fetchowania. |
| `onboarding-picker.tsx` | `OnboardingPicker({ exercises })`, typ `PickableExercise` | Sekcja modala zaproszenia (widok trenera): checkbox „Dołącz formularz startowy" odsłaniający szukajkę + multi-select ćwiczeń (limit `MAX_ONBOARDING_EXERCISES`, licznik wybranych) i notatkę dla podopiecznego. Puste `exercises` pokazuje link do biblioteki zamiast pickera. Nie renderuje `<Form>` — owija go trasa-rodzic (wzorem `consultation-form.tsx`). |
| `exercise-picker.tsx` | `ExercisePicker({ open, onClose, onPick, title, excludeIds?, excludedNote? })`, `filterExercises` | Modal jednokrotnego wyboru ćwiczenia z biblioteki podopiecznego (widok podopiecznego): zamiennik w sesji albo dodatek spoza planu. Biblioteka schodzi leniwie przez `useFetcher` do trasy zasobowej `/biblioteka-cwiczen`, przy pierwszym `open` — renderować NA STAŁE jak `Modal`, na którym stoi, bo `open` przełącza widoczność, nie montaż. `excludeIds` odsiewa ćwiczenie właśnie zastępowane (backend odmówiłby wyborem samego siebie). `excludedNote` (dołożone przy przeglądzie Zadania 8) zastępuje domyślne „Nic nie pasuje do…", gdy szukajka COŚ znalazła, ale wszystko wypadło przez `excludeIds` — bez tego odsiew wygląda jak dziura w bibliotece, nie jak świadome ograniczenie wyboru. `filterExercises` to czysta funkcja szukajki + odsiania, jedyna pokryta testem tutaj — zachowanie modala idzie przez Playwright. |
| `log-exercise-card.tsx` | `LogExerciseCard({ entry, eIdx, totalEntries, maxVideoBytes, onUpdateSet, onVideoStateChange, onSkipSet, onUnskipSet, onCopyFromFirst, onAddSet, onRemoveSet, onSwap, onUndoSwap, onRemoveEntry })` | Karta ćwiczenia w formularzu logowania treningu (podopieczny): nagłówek z celem serii i notatką planu (zamiennik ZACHOWUJE liczbę serii z planu, ale traci cel powtórzeń i notatkę — dziedziczenie celu po zastąpionym ćwiczeniu było błędem, poprawionym przy przeglądzie Zadania 8), „Wypełnij jak #1"; lista serii jako `SetRow` (reps/sekundy, trudność 1–10 barwiona przez `tierFor`, `VideoUploadField` na serię) albo `SkippedSetRow` dla pominiętych — oba prywatne w module. Przyciski karty: „Dodaj serię"/„–" (`onAddSet`/`onRemoveSet`, wyłącznie dla wierszy PONAD plan, sufit klientowi `MAX_SETS_PER_ENTRY = 50`, miękki — gasi tylko przycisk, nie waliduje), „Wymień"/„Cofnij wymianę" (`onSwap`/`onUndoSwap`, wyłącznie dla wpisu z planu — otwiera `ExercisePicker` w trasie-rodzicu), „Usuń" (`onRemoveEntry`, wyłącznie dla wpisu `origin: "extra"`). Plakietki „dropset"/„zamiast: ⟨nazwa⟩"/„spoza planu" czytają `entry.isDropsetItem`/`origin`. **Przeniesiony** bez zmiany zachowania z `app/routes/podopieczny/loguj.$sessionId.tsx` (Zadanie 7 planu „sesja poza planem", 2026-09-08) — więc jego historia zmian zaczyna się wcześniej niż ten plik — a **rozszerzony o wymianę/dodatek/serie ponad plan dopiero Zadaniem 8**, już jako komponent. |

Konwencje wizualne (kolory, typografia, ikonografia): [`../../design-system/README.md`](../../design-system/README.md).

---
Konwencja i zasady aktualizacji dokumentacji: [`../../CLAUDE.md`](../../CLAUDE.md).

## Driver

- **Kształt:** komponenty używane w **więcej niż jednym miejscu**. Kawałek widoku z jednym
  użytkownikiem zostaje przy trasie
- **Reguła nadrzędna:** dane wchodzą **właściwościami**, z loadera. Komponent sięgający sam po
  dane przestaje być testowalny w oderwaniu i wiąże widok z siecią
- **Wygląd:** przez tokeny z `app/styles`, nigdy wartościami wprost. Warstwę wizualną prowadzi
  `frontend-design:frontend-design`
- **Procedura:** `calisthenos-fe:component`
- **Ostatnia rewizja:** 2026-09-06

