# app/lib/ — logika domenowa i infrastruktura

Warstwa domenowa (wywołania kontraktu BE, reguły prezentacji) oraz infrastruktury
(auth, env, formatowanie). Trasy w `app/routes/` wołają te funkcje w loaderach
i akcjach. **Po segmencie S6 wszystkie moduły stoją na kontrakcie** — nie ma już
ani jednego biorącego `db: Db`.

Wynika z tego jedna zasada, o którą łatwo się potknąć, przenosząc stary kod:
moduł na `api: Api` **nie ma zakresu tenanta w sygnaturze**, bo niesie go token
dostępowy, a egzekwuje BE. `traineeId` zostaje wyłącznie tam, gdzie kontrakt ma
go w ścieżce (`/v1/trainees/{traineeId}/…`). Dokładanie `trainerId` podtrzymuje
złudzenie, że FE czegokolwiek pilnuje.

## Driver

- **Kształt:** jedyna warstwa, która rozmawia z backendem. Trasy biorą dane stąd i pilnuje tego
  bramka `app/routes/no-direct-api.test.ts`
- **Reguła nadrzędna:** moduł bierze `api: Api` i **nie ma** argumentu najemcy jako filtra —
  zakres niesie token, egzekwuje backend. Brak autoryzacji daje **404**, nie 403
- **Testy:** test-first przeciw **podstawionemu klientowi** (`createApiClient` z podstawionym
  `fetch`), `<nazwa>.test.ts` obok modułu
- **Procedura:** `calisthenos-fe:lib-module`
- **Ostatnia rewizja:** 2026-09-06

## Pliki w tym katalogu (moduły domenowe i pomocnicze)

| Plik | Rola / kluczowe eksporty |
|---|---|
| `authz.ts` | Reguły dostępu tenant-scope: `ownsTrainerScope`, `canRead`, `canWrite`, typ `Resource`. Stoją na **liście** ról z `api/context.ts` (ADR-0013), więc oba warunki — trener tego tenanta i podopieczny tego trenera — sprawdzane są wprost, nigdy przez `else`: ta sama osoba bywa jednym i drugim naraz, a między okresami ról bywa żadnym. `canRead` pyta o `user.id === resource.trainerId`, nie o samo posiadanie roli trenera. Pilnuje tego `authz.test.ts` tabelą po wszystkich kombinacjach ról i tenantów. |
| `env.ts` | Walidacja i leniwy dostęp do zmiennych środowiskowych (Zod): `getEnv()`, `env`, typ `Env`. Limity uploadu: `MAX_UPLOAD_BYTES` (ogólny, domyślnie 250 MB) i `MAX_VIDEO_UPLOAD_BYTES` (osobny, niższy dla wideo, domyślnie 30 MB). |
| `format.ts` | Formatowanie dat/liczb po polsku: `fmtDate`, `fmtDateShort`, `fmtDateTime` (data+godzina UTC), `fmtTime` (sama godzina UTC), `daysAgo`, `todayISO`, `pluralizePl`; helpery miesiąca kalendarza: `monthRangeUTC` (zakres „YYYY-MM" → ISO UTC + rok/miesiąc), `shiftMonth`. Stała **`APP_TIME_ZONE`** (`Europe/Warsaw`) — jedyne miejsce nazywające strefę aplikacji: godziny trzymamy jako czas ścienny w komponentach UTC, więc na granicy z systemem świadomym stref (Google Calendar) trzeba tę strefę podać jawnie. |
| `file-uploads.ts` | **Wszystkie trzy ścieżki wysyłki na kontrakcie**: `uploadExerciseDemo(api, file)`, `uploadSetVideo(api, file)` i `uploadBodyPhoto(api, file)` — każda dwufazowa (`POST /v1/files/{rodzaj}` → `POST /v1/files/{id}/confirm`) przez wspólną prywatną `uploadThroughContract` (rozmiar i pusty plik sprawdzane PRZED wysyłką, bo plik jest już w pamięci po `request.formData()`; `UploadError` wąsko z `400`/`409`/`413`, reszta leci `ApiError`-em). Rodzaj pliku wynika z **użytej operacji kontraktu**, nie z parametru (`docs/04` §8) — `UploadKind` służy już wyłącznie do wyboru limitu. `maxUploadBytesFor(kind, limits?)` jest lustrem `UPLOAD_LIMIT_SOURCE` po stronie BE: niższy limit wideo dotyczy TYLKO `set_video`, demo trenera i zdjęcie sylwetki chodzą limitem ogólnym (limit surowszy od kontraktu odrzucałby w przeglądarce pliki, które BE przyjmuje). **Moduł nie dotyka już wolumenu w ogóle**: `deleteFileBlob` zniknęło razem z kaskadą `deleteTraineeFully`, którą zastąpiło `DELETE /v1/trainees/{id}` (BE kasuje pliki sam), a wraz z nim katalog `storage/`. Zniknęły bez zamiennika także: `uploadFile`, `UploadCleanupQueue`, `iterateFileChunks`, `deleteFile`, `deleteFileRow`, `findFileById` — zapis na dysk, walidacja magic-bytes i sprzątanie po nieudanym zapisie należą do BE (typ sprawdzany po ZAWARTOŚCI w locie, `orphan-files-sweep` z 24 h karencji). |
| `exercises.ts` | Ćwiczenia — **w całości na kontrakcie** (`api: Api`, żadna funkcja nie stoi już na `db`). **Odczyt:** `listExercisesForTrainer(api, opts)` (biblioteka stronicowana — filtr `q`/`tag`/`unit`, nieznany `tag` ignoruje kontrakt; zawsze `status: "active"`), `listActiveExercisesForTrainer(api)` (picker edytora planu/formularza startowego, sklejone strony kontraktu w jedną listę; celowo BEZ filtra wariantów umiejętności — to osobna lista, `listAssignableExercises` w `skills.ts`), `listActiveExercisesForTrainee(api)` (`GET /v1/me/exercises`, ADR-0038 — picker wymiany/dodatku przy logowaniu treningu; bliźniak wariantu trenera, **z jedną różnicą**: wozi `tracksRpe`, od którego zależy kształt wiersza serii; pętla stron jest CELOWO powielona, nie wydzielona do wspólnego helpera z wariantem trenera, żeby nie ryzykować niezamierzonej zmiany w edytorze planu i formularzu startowym; zapytanie **nie niesie `status`** — ten kontrakt go nie zna i oddałby `400`, status jest po tamtej stronie wymuszony na `active`), `getExerciseDetail(api, exerciseId)` (szczegół do edycji; `\| null` mapuje `404` przez `orNull`). **Zapis:** `createExercise(api, input)` (sekwencja: wysyłka demo → utworzenie → podpięcie; **zwraca `{ id, demoError }`, nie `void`** — odmowa PRZED utworzeniem leci wyjątkiem, bo nic nie powstało i wolno ponowić, a odmowa PO utworzeniu wraca w `demoError`, bo ćwiczenie już istnieje i ponowienie zrobiłoby drugie), `updateExercise(api, input)` (jeden `PATCH`, demo opcjonalne), `setExerciseArchived(api, exerciseId, archived)` (archiwizacja/przywrócenie; `ExerciseError` mapuje wąsko odmowę „wariant umiejętności", każdy inny `409` leci jako `ApiError`). **Cztery rzeczy do zapamiętania:** (1) słownik `CONTRACT_SORT` tłumaczy sortowanie z zakładkowalnego URL-a (`name_asc`/`name_desc`/`newest`/`oldest`) na nazwy kontraktu (`name`/`-name`/`newest`/`oldest`); (2) `demoFileId: null` w `PATCH` ODPINA demo — „zostaw dotychczasowe" to BRAK klucza w ciele, nie `null`; (3) bramka „ćwiczenie jest wariantem aktywnej umiejętności" na archiwizacji jest teraz wyłącznie po stronie BE, przychodzi jako `409 EXERCISE_IS_SKILL_VARIATION` (trasa uprzedza fakt, pokazując `variationOf` ze szczegółu); (4) `demoUrl` z kontraktu jest **ścieżką**, nie adresem — origin dokłada `publicFileUrl` (`api/client.ts`) TUTAJ, w module, nie w trasie, i dotyczy to tak samo szczegółu, jak kafelków listy. Zniknęły bez zamiennika: `countExercisesForTrainer` — dawna para z `listExercisesForTrainer` (dwa zapytania, własne liczenie strony) stała się zbędna, odkąd kontrakt oddaje `total` RAZEM z listą; `normalizeTags`, bo nie miała żadnego wywołania; oraz `countActiveExercisesForTrainer` (licznik nawigacji) — pole `activeExercises` przeszło do `loadTrainerNavigation(api)` w `views.ts`. |
| `views.ts` | Widoki przekrojowe BE, jedno wywołanie na ekran: `loadTrainerNavigation(api)` (`GET /v1/trainer/nav`), `loadTrainerDashboard(api)` (`GET /v1/trainer/home`), `loadTraineeNavigation(api)` (`GET /v1/me/nav`), `loadTraineeDashboard(api)` (`GET /v1/me/home`). Layouty biorą stąd liczniki obszarów już przepiętych; obszar przepinający swój moduł USUWA z layoutu funkcję liczącą i bierze pole stąd. Pulpit trenera bierze stąd już wszystko: klientów, ostatnie treningi, liczniki. |
| `categories.ts` | Kategorie ćwiczeń: `listCategoriesForTrainer`, `addCategory`, `deleteCategory`, `filterToKnownCategoryNames`, `normalizeCategoryName`, `CategoryError`. **Pierwszy moduł przeniesiony z bazy na kontrakt** — bierze `api: Api` tam, gdzie reszta bierze jeszcze `db: Db`, i jest wzorcem dla przepięcia pozostałych ([`api/README.md`](api/README.md)). Trzy rzeczy przy tym zniknęły: parametr `trainerId` (zakres tenanta niesie token, nie argument), `ORDER BY` (kolejność przychodzi z kontraktu wraz z `ordinal`) i kod `23505` z Postgresa jako źródło `CategoryError` — teraz jest nim `409`, mapowane **wąsko**, żeby awaria BE nie pokazała się w formularzu jako zajęta nazwa. |
| `plan-types.ts` | Schematy Zod formularza planu (single/superset/dropset): `PlanFormSchema`, `SessionFormSchema`, `BlockFormSchema`, `ItemFormSchema` + typy. |
| `plans.ts` | **W całości na kontrakcie** (`api: Api`, żadna funkcja nie stoi już na `db`). **Odczyt:** `listPlansForTrainer(api, { status, q?, sort, page })` — cała strona z kontraktu razem z `counts` (liczniki zakładek, zawsze bez zarchiwizowanych, niezależne od filtra i szukajki); `listPlansForTrainee(api, traineeId)` — wszystkie plany pary łącznie z zarchiwizowanymi, malejąco po wersji, bez stronicowania (zasób nie ma rozmiaru strony); cudzy podopieczny to PUSTA lista, nie `404`; `findDraftForTrainee(api, traineeId)` filtruje tamtą listę po `status === "draft"` — jedyne miejsce pytające o szkic pary bez szczegółu planu pod ręką, odbicie w loaderze `plany.nowy.tsx`; `loadPlanForTrainer(api, planId)` — pełne, płaskie drzewo planu (`PlanDetailView`) z nazwą podopiecznego, nazwami ćwiczeń, `draftId` pary i `editable` wyliczonym przez BE; `\| null` w sygnaturze mapuje `404` przez `orNull` (cudzy plan nieodróżnialny od nieistniejącego). **Zapis:** `createBlankPlan(api, { traineeId, name })` → `{ id, created }` (`409 PLAN_DRAFT_EXISTS` wraca jako `created: false` z identyfikatorem istniejącego szkicu, zamiast tworzyć drugi; każdy inny `404`/`409` — w tym `PLAN_DRAFT_EXISTS` bez `details.planId` — leci jako `PlanError` do formularza); `createDraftFromActive(api, id)` → `{ id, created }` z kodu odpowiedzi (leniwy szkic z aktywnego planu); `saveDraftPlan(api, id, form)` zapisuje całe drzewo przez `toSavePlanDto` (wipe & rewrite po stronie BE, tylko dla `draft`); `publishPlan(api, id)` — poprzedni aktywny trafia do archiwum atomowo po stronie BE; `deletePlan(api, id)` → `PlanDeleteOutcome` (`"deleted" \| "archived"`; o wyniku decydują logi, nie status planu — bez liczby logów), `planDeleteOutcomeMessage(outcome)` zamienia to na jedno zdanie sukcesu wspólne dla obu wołających tras; `PlanError` (`userMessage` z koperty BE, mapowany wąsko z `400`/`404`/`409`). **Trzy rzeczy do zapamiętania:** (1) `toSavePlanDto` ZDEJMUJE `id` z drzewa formularza — BE odrzuca pola spoza DTO, a `PlanForm` jest strukturalnie szerszy niż DTO, więc typy tego nie zgłoszą; (2) sortowania (`PlanSort`) są identyczne z kontraktem, więc — inaczej niż w `exercises.ts` — nie ma słownika i nie należy go dopisywać; (3) reguły „jeden szkic na parę", „tylko szkic można zmieniać", „nie publikuj pustego planu" i wyścig przy usuwaniu mieszkają w BE — moduł tylko przekazuje ich komunikaty. Zniknęły bez zamiennika: `countPlansForTrainer`, `countPlansByStatusForTrainer` (oba weszły w `counts` listy); `countPlansForTrainerByStatus` i `countSessionsInPlan` (liczniki przeszły do `views.ts`); `findDraftBasedOn`, `findAnyDraftFor`, `findPlanStatusForTrainer` (szczegół niesie `draftId` i status wprost); typy `PlanDetail`, `PlanListRow`, `DeletePlanResult` z polem `logCount`. |
| `workouts.ts` | **W całości na kontrakcie** (`api: Api`, żadna funkcja nie stoi już na `db`). Kontrakt rozdziela trasy podopiecznego i trenera, więc jedna dawna funkcja z parametrem `viewer` to dziś dwie. **Odczyt:** `listMyLogs(api, { page, sort, q?, video? })` i `listTraineeLogs(api, traineeId, …)` — cała strona z kontraktu (20/stronę, stronę spoza zakresu przycina BE; cudzy podopieczny to pusta strona, nie `404`); `loadMyLog(api, id)` i `loadTraineeLog(api, traineeId, id)` — szczegół z pełnym drzewem serii (`\| null` mapuje `404`; `videoUrl` ze ścieżki kontraktu na adres przez `publicFileUrl` W MODULE; bez liczby oczekiwanych serii — pominięte w środku widać z luk w `ordinal`, o ogonie mówi `allDone`); `loadMyActivePlan(api)` (`GET /v1/me/plan`, `\| null` — brak planu to `404 PLAN_NOT_FOUND`, stan normalny); `loadSessionForLogging(api, sessionId)` (`GET /v1/me/plan/sessions/{id}`, `demoUrl` z originem; sesja planu archiwalnego przechodzi, szkicu — `409 PLAN_NOT_PUBLISHED` leci dalej); czysta `toLoggingEntries(session)` spłaszcza sesję do `LoggingEntry` (dropset: liczba serii z BLOKU; następca niżej). **Zapis:** `saveWorkoutLog(api, input, { idempotencyKey? })` → `CreatedWorkoutLogView` z `personalRecords` — jedno żądanie `POST /v1/workout-logs` z nagłówkiem `Idempotency-Key`, ciało składane jawnie pole po polu (BE odrzuca pola spoza DTO), `WorkoutSaveError` wąsko z `400`/`404`/`409`. **Budowanie ładunku (Zadanie 5):** `toLogEntries(session)` — następca `toLoggingEntries` z pochodzeniem (`origin: "planned" | "substitute" | "extra"`), wskaźnikiem na zastąpione ćwiczenie, stabilnym `key` (zamiast `planItemId`) i `sets` wypełnionymi pustymi wierszami w liczbie `plannedSets` — nadzbiór strukturalny `DraftEntry` (`log-draft.ts`), więc przechodzi bez rzutowania wszędzie tam, gdzie kontrakt woła ten typ; `buildLogPayload(entries: DraftEntry[])` → `LogPayloadExercise[]` liczy ciało `exercises` POZA akcją trasy (dotąd 60 linii wewnątrz jej `try`) — pomija wpis bez wypełnionej serii, zachowuje dziurę w `ordinal` dla serii pominiętej w środku, przenosi `origin`/`substitutedExerciseId` (baza pod `SUBSTITUTED_EXERCISE_ALSO_LOGGED`, N14). `toLoggingEntries`/`LoggingEntry` zostają obok, NIEUSUNIĘTE — jedyny ich konsument, trasa `podopieczny/loguj.$sessionId.tsx`, przechodzi na nowy kształt dopiero w Zadaniu 8; usunięcie ich wcześniej rozjechałoby trasę o więcej błędów typów niż te już tam czekające. **Trzy rzeczy do zapamiętania:** (1) własność nagrań, przynależność ćwiczeń do sesji, reguły RPE, data z przyszłości i pusty trening są regułami BE — moduł tylko niesie ich komunikaty; (2) sortowania i filtry są identyczne z kontraktem, `all` to brak parametru, słownika nie ma; (3) `videoUrl` jest podpisany tożsamością PYTAJĄCEGO — trener i podopieczny dostają różne adresy na to samo nagranie. Zniknęły bez zamiennika: `countLogsForTrainee` (strona niesie `total`), `loadActivePlanSummaryForTrainee`, `listRecentLogsForTrainer`, `countLogsForTrainerSince` (pulpity z `views.ts`), `findActivePlanForTrainee`, `loadActivePlanFullForTrainee` (sesję bierze się po id), `assertOwnedUnclaimedVideos`, `findUnusableVideoIds` (BE), `loadLogForViewer` (dwie funkcje wyżej), `countClientsForTrainer` (`total` przychodzi ze stroną `GET /v1/trainees`); `listClientsForTrainer` przeszła do `trainees.ts`. |
| `stats.ts` | **Przegląd klienta — jedno wywołanie, cały moduł** (`api: Api`): `loadTraineeOverview(api, traineeId)` (`GET /v1/trainees/{traineeId}/overview`) oddaje `TraineeOverviewView` = plan aktywny z wykorzystaniem sesji i sumami, szkic, kondycja współpracy, stagnacja, rozkład tagów, pokrycie nagraniami i zdjęciami. **Siedem funkcji i siedem własnych typów zniknęło** na jego rzecz (`getHealthStats`, `getPlateauExercises`, `getTagDistribution`, `getVideoCoverage`, `getBodyPhotoCoverage`, `getActivePlanSessionUsage`, `getCurrentPlanTotals`) — ekran robił z nich osiem równoległych zapytań. Typy to re-eksporty kontraktu; **nazwy pól są inne** (`PlateauItemView.name`/`recentBest` zamiast `exerciseName`/`recentAvgReps`, `PlanSessionUsageView.name` zamiast `sessionName`) i to ustalenie BE obowiązuje. Bez `orNull`: cudzy podopieczny to `404` na granicy błędu, nie pusty ekran. Zostaje jeszcze alias `HeatmapDay` = `TraineeHeatmapDayItem` — kształt dnia mapy aktywności, którego dane niesie dziś wyłącznie `TraineeHomeView.heatmap` z `views.ts`, a importują go komponenty `stat-widgets.tsx` i `trainee-stats.tsx`. Zniknęły bez zamiennika: `getActivityHeatmap` (**luka L S5-1** — kontrakt nie ma mapy aktywności dla trenera, `docs/03` mówi o niej „jeszcze nie zbudowane"; ekran przeglądu klienta stracił kafelek, świadomie BEZ składania go z dziennika), `getExerciseProgress` i `getEasierAtSameReps` z typami `ExerciseProgress`/`EasierExercise` (martwe od S1, gdy `skill-progression.ts` przestał je importować), a wcześniej `getMonthSummary`, `getTopExerciseSparklines`, `getPersonalRecords`, `MONTH_NAMES`, `detectNewPRsForLog`, `getSideBySidePhotoPairs`. |
| `wrapped.ts` | Miesięczne podsumowania „Wrapped" — **w całości na kontrakcie** (`api: Api`), po tym jak ok. 400 linii agregacji przeszło do BE. `loadWrappedSummary(api, ym)` (`GET /v1/me/wrapped/{ym}`) oddaje `WrappedSummaryView` z sumami, ćwiczeniem wiodącym, rekordami miesiąca, najcięższym dniem, WYBRANYM archetypem i porównaniem m/m; `\| null` mapuje `404` przez `orNull`, bo tak kontrakt odpowiada na miesiąc bez danych (dawna flaga `hasData` nie ma czego opisywać). W FE została **prezentacja**: `describeArchetype(summary)` dokłada do `key`/`emoji` z kontraktu polską nazwę (słownik totalny po `ArchetypeKey`) i opis — cztery z dziewięciu opisów niosą liczby i wszystkie cztery są w odpowiedzi (`prs`, `archetype.newExercises`, `topExercise.pctOfSessions`, `archetype.distinctExercises`), więc żadne zdanie nie zbiegło do wersji ogólnej. Czyste helpery miesiąca zostają bez zmian: `latestWrappedMonth` (najświeższy miesiąc z listy `GET /v1/me/home`, po `ym`, nie po pozycji), `parseYM`, `formatYM`, `monthLabel`, `isPastMonth`. **Uwaga na kontrakt:** generator typuje `path.ym` jako `Array<string>`, choć ścieżka ma jeden segment (luka L S5-4) — opakowanie w `[ym]` stoi w jednym miejscu, w `loadWrappedSummary`. Zniknęły bez zamiennika: `getMonthlyWrapped` z sześcioma prywatnymi zapytaniami, `pickArchetype`, `weeksOverlappingMonth` oraz typy `WrappedSummary`, `MonthlyPR`, `TopExercise`, `HeaviestDay`, `VsPrevious`, `Archetype` w dawnym kształcie (dziś re-eksporty kontraktu); wraz z nimi pole `weeksActive`, którego kontrakt nie niesie, a którego ekran nie pokazywał. |
| `body-photos.ts` | Zdjęcia sylwetki — **w całości na kontrakcie** (`api: Api`, żadna funkcja nie stoi już na `db`). **Odczyt:** `listMyBodyPhotos(api, { page, sort })` — cała strona z kontraktu (60/stronę, `total`/`totalPages`, stronę spoza zakresu przycina BE); `listAllMyBodyPhotos(api)` i `listAllTraineeBodyPhotos(api, traineeId)` — **para funkcji** sklejająca strony w jedną listę od najnowszego zdjęcia (precedens: `listActiveExercisesForTrainer`), bo porównanie „przed / po" musi widzieć WSZYSTKIE zdjęcia ujęcia, a kontrakt nie ma parametru „wszystko"; u trenera ta sama lista karmi też siatkę, która nigdy nie miała stronicowania. **Zapis:** `addBodyPhoto(api, { file, view, takenOn, note })` — dwie fazy (`uploadBodyPhoto` → `POST /v1/me/body-photos`), ciało składane pole po polu; `deleteBodyPhoto(api, photoId)` — wyłącznie WŁASNE zdjęcie, zawartość pliku kasuje BE (ADR-0021), więc nie ma tu transakcji ani sprzątania bloba. **Czysta:** `getSideBySidePhotoPairs(photos)` — pierwsze i ostatnie zdjęcie każdego ujęcia, trzy pozycje ZAWSZE (także dla ujęcia bez zdjęć), skrajne wybierane po `takenOn` z remisem po `createdAt`, więc kolejność wejścia nie ma znaczenia. `BodyPhotoError` (`userMessage` z koperty BE, wąsko z `400`/`404`/`409`; kontrakt nie deklaruje dla sylwetki żadnego kodu znaczącego dla logiki, więc rozgałęzienia po `error.code` nie ma). **Trzy rzeczy do zapamiętania:** (1) `photoUrl` z kontraktu jest ŚCIEŻKĄ — origin dokłada `publicFileUrl` W MODULE, nigdy w trasie ani w komponencie; (2) `sort` z zakładkowalnego adresu (`newest`/`oldest`) tłumaczy słownik `CONTRACT_SORT` na `taken_on_desc`/`taken_on_asc`; (3) galeria trenera niesie w kontrakcie gotowe `pairs`, których moduł **świadomie nie czyta** — nie znają stanów „jedno zdjęcie" i „brak zdjęć", które ekran rysuje (luka L S4-3). Zniknęło bez zamiennika: `countBodyPhotosForTrainee` (strona niesie `total`, a licznik nawigacji `TraineeNavView.bodyPhotos` z `views.ts`), typ `BodyPhotoRow` (zastąpiony `BodyPhotoDto` z kontraktu — bez `fileId` i `mimeType`). |
| `trainees.ts` | Podopieczni — **w całości na kontrakcie** (`api: Api`, żadna funkcja nie stoi już na `db`). **Odczyt:** `listClientsForTrainer(api, { page, sort, q?, plan? })` (`GET /v1/trainees`) — cała strona z `total` (30/stronę, `q` po nazwie ALBO e-mailu, `sessionCount` wyłącznie z treningów u TEGO trenera); `listTraineesOfTrainer(api)` — komplet do pickera planu ze **sklejonych stron** (precedens: `listActiveExercisesForTrainer`), `sort: "name_asc"` jawnie, bo sklejanie ma sens tylko przy porządku stabilnym między żądaniami; zarchiwizowanych odfiltrowuje sam zasób, bo `GET /v1/trainees` to podopieczni z AKTYWNĄ relacją prowadzenia. `findTraineeRef(api, traineeId)` → `TraineeRef \| null` — **obejście, nie rozwiązanie** (luka L S5-2): kontrakt nie ma `GET /v1/trainees/{id}`, a żaden widok, po który dziewięć tras trenera i tak sięga, nazwy nie niesie (wyjątek: `ConsultationView.trainee`). Stoi w jednym miejscu, gotowe do zamiany na jedno `GET`, gdy BE trasę doda; bramki tenanta NIE stanowi — egzekwuje ją BE przy każdej trasie `/v1/trainees/{id}/…`. **Zapis:** `deleteTraineeFully(api, traineeId)` (`DELETE /v1/trainees/{id}`, `204` bez treści) — BE kasuje przez granice kontekstów wraz z planami, dziennikiem, zdjęciami, plikami, postępem umiejętności i odwzorowaniami kalendarza (ADR-0035), więc zamyka się przy okazji luka L S3-2; `TraineeDeleteError` wąsko z `404` (cudzy, były, nieistniejący) i `409 TRAINEE_HAS_OTHER_TIES`. Typy to re-eksporty kontraktu (`TraineeListItem`, `TraineeListPage`, `TraineeRef`). Zniknęły bez zamiennika: `countTraineesOfTrainer` (layout bierze `nav.trainees` z `views.ts` — i liczy dziś tylko aktywnych, decyzja D3), `assertTraineeOwnedBy` (FE nie pre-sprawdza cudzego), `findTraineeOfTrainer`, `getTraineeOfTrainer`; a wraz z kaskadą — `deleteFileBlob` i cały katalog `storage/`. Dwie rzeczy, których kontrakt nie niesie: data dołączenia (zniknęła z nagłówka przeglądu klienta) i nazwa aktywnego planu na liście (jest `hasActivePlan`) — oba są addytywne po stronie BE. |
| `calendar.ts` | Kalendarz zewnętrzny — **jedyne wejście FE do tego obszaru** (`api: Api`): `getCalendarConnection(api)` (`GET /v1/calendar/connection`), `startCalendarAuthorization(api)` (`POST …/authorize` → `{ url, setCookie }`), `disconnectCalendar(api)` (`DELETE …`). Nazwany `calendar.ts`, nie `google.ts` — dostawca jest WARTOŚCIĄ pola `provider`, nigdy częścią nazwy (ADR-0012 BE), a `CalendarConnectionView.provider` jest `string`, nie enumem. **`startCalendarAuthorization` jest jedynym miejscem w całej warstwie klienta, które przenosi ciastko BE do przeglądarki** (`response.headers.getSetCookie()`) — świadomy wyjątek od reguły „warstwa klienta nie przenosi ciastek BE": ciastko z nonce'em jest przeznaczone dla PRZEGLĄDARKI (wiąże zgodę ze `state`), a serwer FE jest tylko po drodze; `trener/integracje.google.tsx` dokleja je (`Headers.append`, bo ciastek bywa więcej niż jedno) do własnego przekierowania na ekran zgody. Zamyka lukę LK1 — patrz [spec](../../docs/superpowers/specs/2026-09-04-zgoda-google-przez-dwa-hosty-design.md) i §7 [planu](../../docs/superpowers/plans/2026-09-03-reszta-app-lib-na-kontrakcie.md). |
| `consultation-recurrence.ts` | Czyste liczenie dat terminów z reguły cyklu (bez DB, bez `Date.now`): `nextOccurrences`, typ `RecurrenceRule`. UTC, okno `[from, from+horizonDays]`. Obsługuje weekly/biweekly (kotwica `weekday`) i monthly (`dayOfMonth` ≤28). |
| `consultation-status.ts` | Jedno źródło prawdy dla prezentacji statusu terminu (czyste, cel testów): `consultationPresentation` (etykieta+ton, świadome roli — `planned` przyszły/miniony), `mostUrgentTone` (kropka dnia), mapy `TONE_TEXT`/`TONE_DOT`, typy `ConsultationTone`/`ConsultationPresentation`. Używane przez OBA panele, by status nie wyglądał inaczej zależnie od widoku. |
| `consultation-types.ts` | Zod + czyste guardy przejść: `ScheduleFormSchema`, `ConsultationDocFormSchema`, `ActionItemFormSchema`, `TraineeActionSchema`, `ConsultationItemStatusSchema` + typy; guardy `canTraineeAct`, `canTrainerReschedule`/`canTrainerCancel`, `canDocument`. |
| `consultation-schedules.ts` | **W całości na kontrakcie** (`api: Api`): `getActiveSchedule(api, traineeId)` — odpowiedź jest OPAKOWANA (`{ schedule: … \| null }`), nie `404`, więc BEZ `orNull`: para bez cyklu to `null`, a `404` (cudzy podopieczny) leci `ApiError`-em; `upsertSchedule(api, { traineeId, form })` — `PUT` idempotentny i różnicowy po stronie BE, do ciała idzie WYŁĄCZNIE kotwica pasująca do częstotliwości (DTO ma `weekday?`/`dayOfMonth?` bez `null`, a formularz niesie oba pola); `deactivateSchedule(api, traineeId)` — powtórne wyłączenie NIE daje `409`; `defaultTitle(iso)` — nagłówek terminu liczony z daty, bo **`title` nie istnieje w `/v1`** (BE nadaje własny, żaden ekran go nie pokazuje), więc obie role liczą go stąd; `ScheduleError` (`400`/`404`/`409` wąsko). Zniknęły bez zamiennika: `ensureOccurrences` i `HORIZON_DAYS` — materializację terminów i przesuwanie horyzontu robi BE (zapis harmonogramu plus praca cykliczna workera). |
| `consultations.ts` | **W całości na kontrakcie** (`api: Api`). Kontrakt NIE rozdziela tras ról — jedno `GET /v1/consultations?from&to` dla obu, o zakresie rozstrzyga token — więc trzy dawne listy to dziś dwie funkcje: `listOccurrencesInRange(api, range)` (siatka miesiąca; trener dostaje wszystkich podopiecznych, podopieczny własne) i `listOccurrencesForTrainer(api, traineeId, { nowISO })` (lista pary, zawężana po `traineeId` **w module**, jednym wywołaniem, nie N; okno `LIST_WINDOW_DAYS` = rok w obie strony, bo kontrakt wymaga zakresu zamkniętego). `loadUpcomingConsultations(api, { nowISO, traineeId? })` zastępuje parę `nextUpcomingForTrainee` + `countPendingForTrainee` jednym wywołaniem — **`pending` liczy tu wyłącznie NADCHODZĄCE `planned`**, bo przeszły `planned` jest dla trenera „do udokumentowania"; licznik nawigacji podopiecznego (z przeszłymi, jak liczył legacy) niesie `TraineeNavView.pendingConsultations` z `views.ts`. Dalej: `getConsultationDetail` (płaski `ConsultationDetail` z `summary` i `actionItems`; `\| null` przez `orNull`), `createAdhocConsultation` (ciało bez `title`, `periodFrom`/`periodTo` — kontrakt ich nie zna; podsumowanie i punkty WYŁĄCZNIE dla `documented`), `documentConsultation`, `rescheduleOccurrence`, `cancelOccurrence`, `respondToOccurrence` (dozwolone przejścia sprawdza BE, więc guard `canTraineeAct` tu nie stoi; notatka wymagana przy `request_change`), `setActionItemStatus` (punkt adresowany przez termin — `id` i `itemId` w ścieżce), `deleteConsultation`, `runConsultationSync(api, traineeId)` (ręczne uzupełnienie zaległości w kalendarzu; `connected: false` to `200`, nie błąd), `ConsultationError`. Czas: `toAppWallClock`/`fromAppWallClock` tłumaczą moment UTC z kontraktu na czas ścienny, na którym stoją komponenty. **Zniknęło bez zamiennika:** sześć helperów synchronizacji z ich typami (`getSyncRow`, `listUnsyncedForSync`, `listSyncedForRepair`, `setGoogleEventId`, `listGoogleEventIdsForPair`, `listCancelledGoogleEventIds`) — zwykłe mutacje niczego nie dosyłają, bo każdą wypycha BE przez outbox. |
| `consultation-form.server.ts` | Parsery FormData → obiekty do walidacji Zodem: `parseConsultationDocFormData` (termin/dokumentacja; punkty jako równoległe `itemBody[]`/`itemStatus[]`), `parseScheduleFormData` (reguła harmonogramu). Tylko server-side. |
| `progression-math.ts` | Czysta logika "Progresja" (bez DB): zakresy dat, agregacja tygodniowa, statusy (`statusFromSessions` komputuje z `best` — rekordu, `classifyStatus`), PR-oznaczanie (`markPrs`), % zmiana, normalizacja, sortowanie listy (`sortProgressionRows`), polski skrót jednostki (`unitLabelPl` mapuje REPS/SEC), filtrowanie wierszy po ID ćwiczenia (`excludeByExerciseId`), wybór ujęcia wykresu dla okresu (`seriesForRange` — tygodniowe dla 6m/all, ale fallback do per-sesja gdy zwinęłoby do <2 punktów, by szerszy okres nie pokazywał mniej niż węższy), typy `ProgressionListRow`, `SessionPoint`, `ChartPoint`, `ProgressionStatus`. |
| `progression.ts` | Warstwa DB dla Progresji: `loadProgressionSessions` (bazowe zapytanie per-sesja, reużywane przez Tasks 4–5; wyklucza zarchiwizowane ćwiczenia), `listProgressionExercises` (lista landing), `todayIso`. `ComparisonSeries` nosi `startValue`/`endValue` (surowe best na starcie i końcu okresu; do tabeli „Konkretnie" w porównaniu). |
| `skill-progression-math.ts` | Czysta logika "Umiejętności" (bez DB): `currentLevelFromEvents` (najświeższe zdarzenie awansu → bieżący wariant), `suggestAdvancement` (miękka sugestia awansu/cofnięcia na podstawie sygnałów), typy `AdvancementEvent`, `AdvanceSignals`, `AdvancementSuggestion`, stałe `MIN_SESSIONS_FOR_SUGGESTION`, `HIGH_RPE`. |
| `skill-tier.ts` | Słownik tierów umiejętności (`basic`/`intermediate`/`advanced`/`expert`): `SKILL_TIERS`, `TIER_LABEL` (etykiety PL), `tierRank`, `canBePrerequisite` (prerekwizyt nie może być trudniejszy), `highestEarnedTier`. Czysta logika, bez DB. |
| `skill-pyramid.ts` | Układ piramidy umiejętności: `buildPyramid` (pas = tier, podrzędy z krawędzi wewnątrz pasa), `orderAndPlace` (kolejność w rzędach przez barycentrum sąsiadów + przydział kolumn; prostuje kręgosłupy prereków do jednej kolumny, zwraca `boardCols`) i `layoutPyramid` (geometria planszy: płyty pełnej szerokości, środki węzłów, wysokość, `cardHalfW` dla routingu). Czysta logika, bez DOM/DB — SSR-clean. |
| `skill-pyramid-routing.ts` | Trasowanie krawędzi piramidy: `routeEdges` (łamana ortogonalna z górnej krawędzi prereka do dolnej krawędzi zależnej; korytarz omijający karty, pasy dla poziomych odcinków, flaga `reversed`) i `edgePathD` (łamana → `d` z zaokrąglonymi zakrętami, osobny promień dla osi X i Y). Czysta logika, bez DOM/DB. |
| `skill-types.ts` | Schematy Zod walidacji formularzy umiejętności: `SkillFormSchema` (pole `tier: SkillTier`, domyślnie `"basic"`), `AdvancementFormSchema`, `ReorderFormSchema`, `PrerequisiteFormSchema` + typy `SkillForm`, `AdvancementForm`, `ReorderForm`. |
| `skills.ts` | Repozytorium umiejętności: `listSkillsForTrainer`, `getSkillWithVariations`, `createSkill`, `updateSkill`, `archiveSkill`, `addVariation`, `removeVariation`, `reorderVariations`, `listAssignableExercises`, `findSkillForExercise`, `SkillError`. `tier: SkillTier` przechodzi przez odczyty (`SkillListRow`, `SkillDetail`, prerequisite listy) i zapisy (`createSkill`/`updateSkill`, ostatni argument); `updateSkill` celowo NIE blokuje zmiany tieru przy istniejących krawędziach. Prerequisite CRUD: `addPrerequisite` (twardo waliduje „prereq nie z wyższego tieru" PRZED odsłonięciem jakiegokolwiek komunikatu — najpierw własność obu umiejętności, potem porównanie tierów), `removePrerequisite`, `listPrerequisitesForSkill`, `listAssignablePrerequisites` (odfiltrowuje kandydatów z wyższego tieru), `listConflictingPrerequisites` (prereki z wyższego tieru, powstałe przez późniejszą zmianę tieru — do ostrzeżenia w edytorze). Tenant-scope przez `trainerId`; UNIQUE(exercise_id) i ON DELETE RESTRICT na awansach obsługiwane jako przyjazne błędy. `addVariation` odrzuca zarchiwizowane ćwiczenie; `removeVariation` przepakowuje `ordinal` (bez dziur); `archiveSkill` czyści krawędzie prerekwizytów w transakcji; `findSkillForExercise` zwraca aktywną umiejętność, której ćwiczenie jest wariantem (blokada archiwizacji ćwiczenia-wariantu). |
| `skill-tree.ts` | Repozytorium drzewa umiejętności: `getSkillTreeForTrainer` (szkielet DAG dla autora), `getSkillTreeForTrainee` (drzewo z per-podopieczny stanami węzłów); typy `TreeNode` (niesie `tier: SkillTier`; układ piramidy liczy `app/lib/skill-pyramid.ts`), `SkillTree`. |
| `skill-tree-math.ts` | Czysta logika drzewa prerekwizytów (bez DB): `wouldCreateCycle`, `assignLayers`, `orderWithinLayer`, `nodeState`, `topoOrder`; typy `Edge`, `NodeState`. |
| `skill-progression.ts` | Repozytorium postępów umiejętności: `getSkillMapForTrainee` (mapa wszystkich umiejętności trenera z bieżącym wariantem podopiecznego, historią i opcjonalną sugestią awansu; `SkillMapEntry` niesie `tier: SkillTier`), `setStartingLevel` (pierwszy wpis: fromVariationId = null), `recordAdvancement` (awans/cofnięcie; sprawdza "no start" i "same level"). Współdzielone trener/podopieczny. |
| `list-params.ts` | Parser i helper dla kontrolek list (sortowanie, filtrowanie, szukajka): `parseListControls`, `buildControlHref`, typy `ListControlsSpec`, `ListControlsState`, `SortOption`, `FilterGroup`, `FilterOption`. |
| `log-draft.ts` | Szkic logu treningu w `sessionStorage` (czysty, bez DB): `serializeDraft(planExerciseIds, entries)`, `parseDraft(raw, { planExerciseIds })` (odrzuca szkic, gdy PLAN — lista jego ćwiczeń w tej samej kolejności — nie pasuje), `draftHasContent(entries)`, `draftKey`, typy `DraftEntry`, `SetDraft`. Chroni wpisane serie przed utratą, gdy wysyłka formularza logowania zerwie się (`Failed to fetch`) i ErrorBoundary odmontuje komponent. **Wersja 4**: szkic wozi WPISY (`DraftEntry[]`), nie samą macierz serii — lista ćwiczeń formularza przestała być odbiciem planu (wymiana ćwiczenia W MIEJSCU, dodatek spoza planu), więc każdy wpis niesie własne `exerciseName`/`unit`/`tracksRpe`, `origin` (`planned`/`substitute`/`extra`, dla `substitute` razem z `substitutedExerciseId` zastąpionego ćwiczenia) i własną, zmienną liczbę serii; liczba serii przestała być kryterium zgodności, zostało nim wyłącznie `planExerciseIds`. **Szkice v3 (i starsze) są odrzucane CICHO** — `parseDraft` oddaje `null`, formularz wstaje pusty, bez komunikatu i bez śladu w logach; ta sama decyzja co przy odrzuceniu v2, bo szkic żyje tylko przez sesję przeglądarki i migracja nie jest warta kodu. Używane w `routes/podopieczny/loguj.$sessionId.tsx` (zapis/przywracanie) i `historia.$logId.tsx` (czyszczenie po udanym zapisie, wyłącznie przez `draftKey`). |
| `money.ts` | Kwota ustaleń w groszach: `parsePlnToGrosze` (string z PLN → grosze lub null), `MonthlyAmountSchema` (Zod; min 200 gr, max 10 000 000 gr, int). **Od 2026-09-08 NIE MA ANI JEDNEGO KONSUMENTA** i czeka na usunięcie. Przeżył pierwsze wycofanie płatności, bo `POST /v1/invites` przyjmowało wtedy `monthlyAmountGrosze` — kontrakt przestał je przyjmować w `0.4.0` (ADR-0037), więc formularz zaproszenia stracił pole, a ten moduł ostatniego użytkownika. `fmtMoney` zniknęło w S6 — czytały je wyłącznie kasowane ekrany Stripe'a, a nic w aplikacji nie wyświetla już kwoty. |
| `feature-request-types.ts` | Zgłoszenia podopiecznych („Pomysły") — czyste, bez DB: `FEATURE_REQUEST_KINDS`/`FEATURE_REQUEST_STATUSES` + etykiety PL (`KIND_LABEL`, `STATUS_LABEL`), `statusPresentation` (etykieta + ton plakietki — jedno źródło wyglądu dla OBU paneli, wzorem `consultation-status.ts`), mapy `TONE_TEXT`/`TONE_DOT`, `canTraineeDelete` (autor kasuje tylko status `new`), Zod `FeatureRequestFormSchema` (tytuł 3–120, opis 10–2000, trim przed liczeniem długości) i `FeatureRequestResponseSchema` (pusta odpowiedź → `null`). Tuple trzymają parzystość z pgEnum — pilnuje jej test jednostkowy. |
| `feature-requests.ts` | Zgłoszenia podopiecznych („Pomysły") — **w całości na kontrakcie** (`api: Api`, żadna funkcja nie stoi już na `db`). Kontrakt rozdziela perspektywy trasami, więc para funkcji tam, gdzie do integracji była jedna z filtrem tenanta w `WHERE`. **Autor:** `listForTrainee(api, { page, sort, status? })` (`GET /v1/me/feature-requests`) — cała strona z kontraktu (20/stronę, `total`/`totalPages`, stronę spoza zakresu przycina BE; wiersze BEZ autora, bo autorem jest pytający); `createFeatureRequest(api, { kind, title, body })` → `FeatureRequestView` (trener wynika z konta autora po stronie BE, nigdy z ładunku; `400` → `FeatureRequestError`); `deleteFeatureRequest(api, id)` (`DELETE`, wyłącznie w stanie `new` — warunek egzekwuje BE przy zapisie, więc trener odpowiadający w tej samej chwili nie przegrywa wyścigu; zero usuniętych wierszy to po tamtej stronie `404`, nie `409` — „nie istnieje, cudze albo już nie `new`" wygląda tak samo — mapowane na `FeatureRequestError`, żeby kliknięcie w nieaktualny przycisk skończyło się zdaniem przy liście, nie ekranem błędu). **Trener:** `listForTrainer(api, { page, sort, status?, kind?, q? })` (`GET /v1/feature-requests`; każdy wiersz z `authorId`/`authorName`, szukajka po tytule, treści i nazwie autora biegnie po stronie BE), `getForTrainer(api, id)` (`\| null` mapuje `404` przez `orNull`), `respondToFeatureRequest(api, { id, status, response })` (`POST …/{id}/response`; datę odpowiedzi stempluje BE wyłącznie przy niepustej treści, a pusta kasuje treść RAZEM z datą — `null` z formularza to BRAK klucza `response` w ciele, bo DTO zna wyłącznie `string`; `400` → `FeatureRequestError`, `404` leci dalej i trasa robi z niego 404). `FeatureRequestError` (`userMessage` z koperty BE). **Do zapamiętania:** sortowania (`newest`/`oldest`) i filtry są identyczne z kontraktem, `all` to brak parametru, słownika nie ma; typy wierszy to re-eksporty DTO (`FeatureRequestView`, `TrainerFeatureRequestView` i strony) — pola nazywają się `createdAt`/`respondedAt` (nie `…ISO`) i `authorName` (nie `traineeName`). Zniknęły bez zamiennika: `countForTrainee`, `countForTrainer` (strona niesie `total`), `countNewForTrainer` (layout trenera bierze `nav.newFeatureRequests`, layout podopiecznego `nav.featureRequests` — oba z `views.ts`), własne typy `TraineeRequestRow`/`TrainerRequestRow`/`TrainerRequestDetail`. |
| `onboarding-form-types.ts` | Formularz startowy — czysta warstwa (bez DB, bez `Date.now`): `answerLabel` (JEDYNE źródło opisu wyniku pozycji — „12 powtórzeń"/„35 s"; to co innego niż skrót osi wykresu `unitLabelPl` z `progression-math.ts`), Zod `OnboardingTemplateSchema` (szablon trenera: 1–`MAX_ONBOARDING_EXERCISES` unikalnych ćwiczeń + notatka), `OnboardingAnswerSchema`/`OnboardingAnswersSchema` (odpowiedzi podopiecznego; tablica ma limit GÓRNY `MAX_ONBOARDING_EXERCISES`, nie tylko dolny — bez niego jedno żądanie z tysiącami powtórzonych `itemId` rozkręcało pętlę UPDATE-ów w repo), `toAnswersInput` (parser równoległych pól `itemId`/`value`/`comment` → obiekt do walidacji; puste pole wyniku → `NaN`, nie `0`, żeby nie zapisać po cichu „ani razu"), typ `OnboardingUnit`, stałe `MAX_ONBOARDING_*`. |
| `onboarding-forms.ts` | Formularz startowy — **w całości na kontrakcie** (`api: Api`; `traineeId` został wyłącznie tam, gdzie kontrakt ma go w ścieżce). **Podopieczny:** `getPendingFormForTrainee(api)` (`GET /v1/me/onboarding-form` — wyłącznie OCZEKUJĄCY formularz z pozycjami i zamrożoną jednostką; wypełniony i nigdy niedoczepiony to jedno `404`, tu `null` przez `orNull`); `hasPendingOnboarding(api)` — bramka wołana JAWNIE z `podopieczny/_layout.tsx` i `wrapped.$ym.tsx` przed czymkolwiek innym (jedno `GET`, `404` → `false`, każdy inny błąd leci dalej — awaria BE nie może wyglądać jak brak formularza). BE ma bramkę globalną `403 ONBOARDING_FORM_PENDING` (ADR-0025), którą `toRouteResponse` zamienia na to samo przekierowanie; jawna bramka zostaje, bo `wrapped` do fali 2 nie woła żadnej innej trasy kontraktu, a layout ma odsyłać ZANIM policzy cokolwiek (`loadTraineeNavigation` jest tam owinięte w `toRouteResponse` jako siatka). `submitOnboardingForm(api, answers)` (`POST /v1/me/onboarding-form`, ciało składane pole po polu; formularz wybiera BE po tożsamości z tokenu i względem NIEGO sprawdza identyfikatory pozycji; `409 ONBOARDING_FORM_ALREADY_COMPLETED`, `409 ONBOARDING_FORM_INCOMPLETE` — niezmienniki domenowe BE mapuje na `409`, nie `400` — i `400` → `OnboardingFormError`, `404` leci dalej). **Trener:** `getFormForTrainer(api, traineeId)` (`GET /v1/trainees/{traineeId}/onboarding-form` → `TrainerOnboardingFormResponse` z wartościami, komentarzami, obiema notatkami i `completedAt`; cudzy podopieczny i formularz niedoczepiony to `404` → `null`), `getFormStatusForTrainee(api, traineeId)` — projekcja TEJ SAMEJ odpowiedzi do plakietki przeglądu klienta (`null` brak / `completedAtISO: null` czeka / data — wypełniony); kontrakt nie ma osobnej trasy statusu, a kształt zostaje do przepięcia przeglądu w fali 2 (S5). `OnboardingFormError` (`userMessage` z koperty BE); typy DTO re-eksportowane z kontraktu. Zniknęły bez zamiennika: `createOnboardingForm` i `attachFormToTrainee` — formularz jedzie w ciele `POST /v1/invites` (`createInvite` w [`auth/invite.ts`](auth/README.md)), a do konta doczepia go BE przy przyjęciu zaproszenia (`attachFormToTrainee` przeżyło jako prywatny pomocnik `consumeInvite` w `auth/invite.ts`, do S6); własne typy `PendingFormView`/`TrainerFormView`/`OnboardingItemView`. Reguły „komplet albo nic", „jedno wysłanie", własność ćwiczeń w szablonie i zamrożenie jednostki mieszkają w BE — moduł niesie ich komunikaty. |
| `logger.ts` | Structured logging (JSON-lines → stdout/stderr): `logger.{info,warn,error}(event, ctx?)`, czysta `formatLogLine` + `errorMeta` (whitelist `name`+`code`, NIGDY `message`/sekretów; płytka auto-redakcja wartości `Error` w ctx), `logUnhandled(error, request)` (logika hooka `handleError` z `entry.server.tsx`; pomija anulowane żądania). Logger nigdy nie rzuca (fallback przy błędzie serializacji). |

## Podkatalogi

| Katalog | Zawartość |
|---|---|
| [`api/`](api/README.md) | Klient backendu `calisthenos-be` (typ `Api`, mapowanie błędów na `ApiError`), sesja na tokenach w ciastku `__Host-kth_api`, jedność rotacji tokenu i middleware, które jako **jedyne** dotyka sesji. Trasy biorą stąd `requireUser(context)` → `{ api, user }` — synchronicznie, bez sieci. Docelowo zastępuje `db/` jako źródło danych; do czasu przepięcia modułów oba katalogi istnieją obok siebie. |
| [`auth/`](auth/README.md) | **Już nie sesje ani hasła** — te przeszły do [`api/`](api/README.md) w kroku 2 Etapu 2. **Wystawianie zaproszeń stoi na kontrakcie**: `createInvite(api, { displayName, email, onboardingForm })` (`POST /v1/invites` — zaproszenie i formularz startowy atomowo po stronie BE; `InviteError` wąsko z `400`/`404`/`409`; oddaje `token`, `url` i `expiresAt`) oraz **podgląd zaproszenia**: `previewInvite(api, token)` (`GET /v1/invites/{token}`, `null` na `404`). Po S6 nic więcej tu nie ma — przyjmowanie zaproszenia (`consumeInvite`/`findInviteByToken`/`hashToken`), odczyty użytkowników (`users.ts`) i hasła (`password.ts`) zniknęły razem z bazą. |

---
Konwencja i zasady aktualizacji dokumentacji: [`../../CLAUDE.md`](../../CLAUDE.md).
